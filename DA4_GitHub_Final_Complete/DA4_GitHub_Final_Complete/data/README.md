# Data

Place the two supplied dataset ZIP files here or in a location visible to the notebook:

- `archive (1).zip` — JanataHack Customer Segmentation
- `bank+marketing.zip` — UCI Bank Marketing

The notebook extracts the archives automatically.
